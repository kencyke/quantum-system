# Refer for explanation to following link:
# https://lefthook.dev/configuration/

# Validate commit messages
commit-msg:
  commands:
    "lint commit message":
      run: npx commitlint --edit {1}

# Scan for secrets in staged files
pre-commit:
  parallel: true
  jobs:
    - run: gitleaks git --verbose --staged --pre-commit
